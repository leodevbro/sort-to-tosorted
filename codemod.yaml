schema_version: "1.0"

name: "sort-to-tosorted"
version: "0.1.4"
description: "Codemod to convert `[...array].sort()` into `array.toSorted()` (ES2023)"
author: "leodevbro <leodevbro@gmail.com>"
repository: "https://github.com/leodevbro/sort-to-tosorted"
license: "MIT"


targets:
  # Keep this aligned with the files matched in workflow.yaml.
  languages: ["typescript"]

keywords: ["transformation", "migration"]

registry:
  access: "public"
  visibility: "public"

capabilities: []

workflows:
  - name: main
    default: true
    path: workflow.yaml
